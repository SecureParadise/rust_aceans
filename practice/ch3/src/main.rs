fn main() {
    let x = 5;
    // data types

    // Scalar :

    // integer: i8,i16,i32,i64,i128,isize,u8,u32 ..... usize
    // Deciaml : 98_222, Hex:0xff,Octal: 0o77,Bianry:0b111_000,Byte(u8 only):b'A'
    //Float:  f8,f16,f32,f64,f128,fsize
    // bool: true, false
    // character: 'a','1'

    // Compound Data Types
    // tuples:      let tupe:(i32, f64, u8) = (500, 6.4, 1);

    // array:        let a:[i32;5] = [1, 2, 3, 4, 5];
    // also          let a = [1, 2, 3, 4, 5];
    // to access array element a[0],a[1]
    println!("Hello, world!{x}");
}
